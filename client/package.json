{
    "scripts": {
        "test": "jest --verbose",
        "coverage": "jest --coverage"
    },
    "devDependencies": {
        "jest": "^28.1.3",
        "jest-environment-jsdom": "^28.1.3",
        "jest-fetch-mock": "^3.0.3"
    }
}
